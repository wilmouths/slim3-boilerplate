{% extends "layouts/layout.html.twig" %}

{% set title = 'Mon compte' %}

{% block content %}

    <div class="row">
        <h2 class="col s12">Mon compte</h2>
        <div class="divider"></div>
        <div class="section">
            <table class="bordered centered striped highlight">
                <thead>
                    <tr>
                        <th>Prénom</th>
                        <th>Nom</th>
                        <th>Email</th>
                    </tr>
                </thead>
                <tbody>
                    <tr>
                        <td>{{ user.first_name }}</td>
                        <td>{{ user.last_name }}</td>
                        <td>{{ user.mail }}</td>
                    </tr>
                </tbody>
            </table>
        </div>
    </div>
{% endblock %}